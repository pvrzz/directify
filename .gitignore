node_modules/
dist/
dist-electron/
out/
release/
.next/
*.log
.env
.env.local
.DS_Store

# Local editor/agent tooling config and design-process notes, not part of the project
.claude/
.tastemaker/

# The website is closed-source and hosted separately from this repo
apps/web/

# Staging copies of fetched icons, already mirrored into the app itself
design/

# Generated against the full local monorepo (including the closed-source website),
# which pulls in platform binaries this repo doesn't need. A fresh clone of just
# apps/desktop + packages/* regenerates its own minimal lockfile via `pnpm install`.
pnpm-lock.yaml
